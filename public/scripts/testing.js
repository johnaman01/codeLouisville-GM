//Testing
 //to attempt programs 

//Create function that binds result to appropriate newListItem	if newListItem.textContent = result
  //then change the color of that newListItem ROW, which is to  become an instance of creature;
  //then newListItem.backgroundColor == yellow;



  


//Set the diceSize from the Math.randomNumber function to test condition (i.e. i <= diceSize)

